apiVersion: apps/v1
kind: Deployment
metadata:
  name: oetzit-database-deployment
spec:
  template:
    spec:
      containers:
        - name: pg
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: oetzit-database-credentials
                  key: database
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: oetzit-database-credentials
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: oetzit-database-credentials
                  key: password
